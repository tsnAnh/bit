import 'package:flutter/material.dart';

class BitEmptyWidget extends StatelessWidget {
  const BitEmptyWidget({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
