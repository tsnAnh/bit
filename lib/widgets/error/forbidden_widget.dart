import 'package:flutter/material.dart';

class ForbiddenWidget extends StatelessWidget {
  const ForbiddenWidget({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
