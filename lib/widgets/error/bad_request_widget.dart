import 'package:flutter/material.dart';

class BadRequestWidget extends StatelessWidget {
  const BadRequestWidget({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
